



const navItems = [...navData];

const headerPhrase= "Page Contents Not Available. Return to Home Page.";

window.addEventListener("load", page);

function page(){
    headerTemplate(headerPhrase);

    // ###

    footer();

} 